# 2026-04-09 v0.2.0

## 波次 / Step

- Wave B
- Step 05 / Step 06 收口

## Changed

- 补齐 Step 05 的代码-文档-发布闭环，正式确认 terminal-core + adapter + workbench pane 基线。
- 新增 Session Runtime、Replay Store、SQLite 索引与 Session Center 消费模型。
- 统一 README、架构索引、Step 索引与能力矩阵的当前事实，清理历史乱码文档。
- 根 `package.json`、前端 apps/packages、Cargo workspace 与 `src-tauri/tauri.conf.json` 统一升级到 `0.2.0`。

## Fixed

- 修复 Session 真相仍可能滞留前端的问题，改为 runtime 持有、UI 只持 attachment。
- 修复 Step 05 缺失 release/review 回写导致的发布闭环断层。
- 修复关键文档历史乱码导致的架构基线不可读问题。

## Architecture Alignment

- `docs/架构/06-终端会话、运行目标与协议设计.md`
- `docs/架构/07-终端工作台与 CLI 原生集成设计.md`
- `docs/架构/08-渲染、兼容性与终端体验设计.md`
- `docs/架构/09-数据、状态与配置治理设计.md`
- `docs/架构/10-性能、可靠性与可观测性设计.md`
- `docs/step/README.md`
- `docs/step/90-架构能力-Step-代码目录-证据映射矩阵.md`

## Verified

- `pnpm install`
- `node --test tests/workspace-structure.test.mjs`
- `node --experimental-strip-types --test tests/runtime-contracts.test.ts`
- `node --experimental-strip-types --test tests/terminal-core-workbench.test.ts`
- `node --experimental-strip-types --test tests/session-center.test.ts`
- `pnpm typecheck`
- `pnpm build`
- `cargo test --workspace`
- `cargo check --manifest-path src-tauri/Cargo.toml`

## Risks

- `Step 07-08` 仍未完成，SSH / Docker / Kubernetes 连接器与 AI CLI Native Host 尚未进入真实主链。
- 当前 Session Recovery 仍是本地 runtime 基线，跨节点恢复与服务端 broker 能力需在后续波次完成。

## Rollback

- 回退到 `v0.1.2` 可恢复 Step 04 桌面桥接基线。
- 若后续 `Step 07-08` 调整 Session / Replay / Connector 边界，必须同步回写 `docs/架构`、`docs/step`、`docs/review` 与 `docs/release`。
