# 2026-04-09 v0.2.2

## 波次 / Step

- Wave B
- Step 07 进行中：完成 Session draft 与 SSH / Docker / Kubernetes launch plan 子阶段

## Changed

- `packages/sdkwork-terminal-types` 新增 `SessionCreateDraft`。
- `packages/sdkwork-terminal-resources` 新增 launchable target 的 Session draft 派生。
- `crates/sdkwork-terminal-resource-connectors` 新增 SSH / Docker Exec / Kubernetes Exec 的 CLI `connect / exec / diag / close` 计划生成。
- README、架构、Step、review、release 文档全部回写到当前真实状态。
- 根 `package.json`、前端 apps/packages、Cargo workspace 与 `src-tauri/tauri.conf.json` 统一升级到 `0.2.2`。

## Fixed

- 修复 Resource Center 只能给出状态、无法表达 Session 创建准备的问题。
- 修复 connector crate 停留在 catalog 层、无法生成真实 CLI plan 的缺口。
- 修复 Step 07 文档未体现 launch plan 子阶段事实的问题。

## Architecture Alignment

- `docs/架构/README.md`
- `docs/架构/04-技术选型与可插拔策略.md`
- `docs/架构/05-功能架构与核心业务流程.md`
- `docs/架构/06-终端会话、运行目标与协议设计.md`
- `docs/架构/17-能力到API与IPC调用矩阵.md`
- `docs/step/README.md`
- `docs/step/07-SSH、Docker、Kubernetes与Remote-Runtime连接器落地.md`
- `docs/step/90-架构能力-Step-代码目录-证据映射矩阵.md`

## Verified

- `node --test tests/workspace-structure.test.mjs`
- `node --experimental-strip-types --test tests/runtime-contracts.test.ts`
- `node --experimental-strip-types --test tests/terminal-core-workbench.test.ts`
- `node --experimental-strip-types --test tests/session-center.test.ts`
- `node --experimental-strip-types --test tests/resource-center.test.ts`
- `pnpm typecheck`
- `pnpm build`
- `cargo test --workspace`
- `cargo check --manifest-path src-tauri/Cargo.toml`

## Remaining Gaps

- 真实 SSH / Docker / Kubernetes 外部命令执行与 smoke 仍未落地。
- Remote Runtime 仍停留在 contract / catalog 阶段，未进入 launch 或 broker 路由闭环。
- Step 07 仍未达到 `L3` 完成定义。

## Rollback

- 回退到 `v0.2.1` 可恢复 Step 07 的 contract / catalog / resource model 基线。
- 若继续推进 Step 07 的外部执行链，必须继续同步更新 `docs/架构`、`docs/step`、`docs/review` 与 `docs/release`。
