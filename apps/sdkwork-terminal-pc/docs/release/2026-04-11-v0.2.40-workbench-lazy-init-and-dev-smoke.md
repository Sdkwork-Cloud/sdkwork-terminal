# 2026-04-11 v0.2.40 Workbench Lazy Driver Initialization and Desktop Dev Smoke

## Scope

- Workbench package render-cost hardening
- Repeated stage/driver factory churn removal
- Desktop `pnpm dev` startup evidence
- Terminal iteration baseline tightening

## Changed

- `packages/sdkwork-terminal-workbench/src/index.tsx`
  - Replaced eager `useRef(createWorkbenchTerminalStage())` with lazy ref initialization.
  - Replaced eager `useRef(createXtermViewportDriver())` with lazy ref initialization.
  - Bound render and attach paths to stable local `stage` and `xtermDriver` handles.
- `tests/terminal-core-workbench.test.ts`
  - Added regression coverage that workbench stage and xterm driver refs stay lazily initialized.

## Fixed

- Fixed workbench render paths recreating stage and xterm driver factories on every render even though only the first instances were retained.
- Reduced the chance that workbench consumers reintroduce the same render-time allocation pressure already removed from the main shell terminal stage.
- Confirmed that the real desktop dev path is live: the Vite host becomes reachable and the Tauri desktop host process launches.

## Verified

- `node --experimental-strip-types --test tests/terminal-core-workbench.test.ts`
- `node --experimental-strip-types --test tests/shell-runtime.test.ts tests/shell-tabs.test.ts tests/shell-app-render.test.ts tests/terminal-view-driver.test.ts tests/terminal-core-workbench.test.ts`
- `pnpm typecheck`
- `pnpm tauri:check`
- Controlled smoke:
  - `node tools/scripts/run-tauri-dev.mjs`
  - verified `http://127.0.0.1:1420` responded
  - verified `sdkwork-terminal-desktop-host.exe` started

## Notes

- This increment does not change package versions, runtime protocols, or desktop window behavior.
- The next highest-value step remains a live PTY interaction smoke for caret position, input echo, `Enter`, `Backspace`, and scroll behavior inside the real terminal surface.
