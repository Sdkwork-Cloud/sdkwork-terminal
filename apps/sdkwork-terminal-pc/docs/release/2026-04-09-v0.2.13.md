# 2026-04-09 v0.2.13

## 波次 / Step

- Wave B
- Step 07 进行中：完成 desktop session replay slice bridge 与 Session Center replay evidence projection 子阶段

## Changed

- `packages/sdkwork-terminal-infrastructure/src/index.ts` 新增 `DesktopSessionReplayRequest`、`DesktopSessionReplaySnapshot` 与 `sessionReplay` bridge。
- `src-tauri/src/lib.rs` 新增 `DesktopSessionReplaySnapshot`、`DesktopRuntimeState::session_replay_slice` 与 `desktop_session_replay_slice`。
- `packages/sdkwork-terminal-sessions/src/model.ts` 新增 replay slice / preview 投影；`apps/desktop/src/session-center.ts` 已将 `desktop_session_index / desktop_session_replay_slice` 组合为 Session Center 快照。
- `packages/sdkwork-terminal-sessions/src/index.tsx` 已在 `SessionsPanel` 展示只读 replay 摘要。
- 根 `package.json`、前端 apps/packages、Cargo workspace 与 `src-tauri/tauri.conf.json` 统一升级到 `0.2.13`。

## Fixed

- 修复 desktop bridge 无法把 `SessionRuntime::replay` 暴露给前端的问题。
- 修复 Session Center 只能看到 session truth、看不到最小 replay evidence 的问题。
- 修复 Step 07 文档未同步 replay slice 子阶段事实的问题。

## Architecture Alignment

- `README.md`
- `docs/架构/README.md`
- `docs/架构/05-功能架构与核心业务流程.md`
- `docs/架构/06-终端会话、运行目标与协议设计.md`
- `docs/架构/17-能力到API与IPC调用矩阵.md`
- `docs/step/README.md`
- `docs/step/07-SSH、Docker、Kubernetes与Remote-Runtime连接器落地.md`
- `docs/step/90-架构能力-Step-代码目录-证据映射矩阵.md`
- `docs/review/2026-04-09-step07-desktop-session-replay-bridge验证.md`

## Verified

- `node --experimental-strip-types --test tests/desktop-runtime-bridge.test.ts tests/desktop-resource-launch.test.ts tests/resources-panel.test.ts tests/desktop-session-center.test.ts tests/session-center.test.ts`
- `pnpm test`
- `pnpm typecheck`
- `pnpm build`
- `cargo test -p sdkwork-terminal-session-runtime`
- `cargo test --manifest-path src-tauri/Cargo.toml`

## Remaining Gaps

- 当前 `SessionsPanel` 已具备只读 replay 摘要，但仍未提供 attach / detach / replay 游标操作入口。
- 远程目标 interactive terminal attach、实时 streaming 与 Remote Runtime broker / node 主链仍未闭环。
- Step 07 仍未达到 `L3` 完成定义。

## Rollback

- 回退到 `v0.2.12` 可恢复 Step 07 的 desktop session index bridge 基线。
- 若继续推进 interactive attach / replay cursor 主链，必须继续同步更新 `docs/架构`、`docs/step`、`docs/review` 与 `docs/release`。
