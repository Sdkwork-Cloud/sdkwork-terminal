# 2026-04-09 v0.1.2

## 波次 / Step

- Wave A
- Step 04 收口

## Changed

- 打通 `Tauri Host -> Local Session Daemon -> Desktop Runtime Bridge` 最小主链。
- 新增 desktop bridge client、readiness 聚合与 daemon 控制区块。
- 根 `package.json`、前端 apps/packages 与 Cargo workspace 统一升级到 `0.1.2`。

## Fixed

- 修复 runtime bridge 描述类型过宽导致的 `pnpm typecheck` 失败。
- 修复 `src-tauri` 缺失协议依赖导致的 Rust 编译失败。
- 修复 Tauri command 根模块注册冲突。

## Verified

- `pnpm install`
- `cargo test -p sdkwork-terminal-session-runtime`
- `node --experimental-strip-types --test tests/desktop-runtime-bridge.test.ts`
- `pnpm typecheck`
- `cargo test --workspace`
- `cargo check --manifest-path src-tauri/Cargo.toml`
