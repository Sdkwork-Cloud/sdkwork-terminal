# 2026-04-09 v0.2.7

## 波次 / Step

- Wave B
- Step 07 进行中：完成 connector launch resolution 子阶段

## Changed

- `crates/sdkwork-terminal-session-runtime` 新增 `ConnectorLaunchResolution` 与 `SessionRuntime::resolve_connector_launch`
- connector-backed Session 在 attach / output 期间保持 `Starting`，不再被提前抬升为 `Running`
- connector 启动成功时可显式收口到 `Running`，并写入 replay `state` 证据
- connector 启动失败时可显式收口到 `Failed`，并写入 replay `warning` 证据
- 根 `package.json`、前端 apps/packages、Cargo workspace 与 `src-tauri/tauri.conf.json` 统一升级到 `0.2.7`

## Fixed

- 修复 connector-backed Session 仍可能被 attach / output 提前改写状态、导致 `Starting` 真相漂移的问题
- 修复 runtime 缺少 connector 启动成功/失败显式收口接口、无法沉淀 replay 证据的问题

## Architecture Alignment

- `README.md`
- `docs/架构/README.md`
- `docs/架构/05-功能架构与核心业务流程.md`
- `docs/架构/06-终端会话、运行目标与协议设计.md`
- `docs/架构/17-能力到API与IPC调用矩阵.md`
- `docs/step/README.md`
- `docs/step/07-SSH、Docker、Kubernetes与Remote-Runtime连接器落地.md`
- `docs/step/90-架构能力-Step-代码目录-证据映射矩阵.md`
- `docs/review/2026-04-09-step07-launch-resolution验证.md`

## Verified

- `cargo test -p sdkwork-terminal-session-runtime`

## Remaining Gaps

- Remote Runtime 仍未进入 broker / node 路由闭环
- 远程目标创建 Session 的真实 connect / exec 编排仍未打通
- Step 07 仍未达到 `L3` 完成定义

## Rollback

- 回退到 `v0.2.6` 可恢复 Step 07 的 runtime launch intent 基线
- 若继续推进远程目标 Session 主链，必须继续同步更新 `docs/架构`、`docs/step`、`docs/review` 与 `docs/release`
