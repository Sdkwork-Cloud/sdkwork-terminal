# 2026-04-09 v0.1.1

## 波次 / Step

- Wave A
- Step 02 命名与结构收口

## Changed

- `packages/` 目录统一为 `packages/sdkwork-terminal-xxx`。
- `package.json.name` 统一为 `@sdkwork/terminal-xxx`。
- 根 `package.json`、前端 packages、apps 与 Cargo workspace 统一升级到 `0.1.1`。

## Fixed

- 清理旧短目录包，避免 workspace 双目录并存。
- 修复 contracts surface helper 的类型问题。
- 回写架构与 step 文档，明确目录命名标准与包名标准。

## Verified

- `pnpm install`
- `node --test tests/workspace-structure.test.mjs`
- `node --experimental-strip-types --test tests/runtime-contracts.test.ts`
- `pnpm typecheck`
- `cargo test --workspace`
- `cargo check --manifest-path src-tauri/Cargo.toml`
