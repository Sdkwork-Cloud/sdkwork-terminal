# 2026-04-10 v0.2.40 Terminal Truncation VT Clean

## Summary

本轮继续停留在 `Wave B / Step 05`，修复 runtime 截断策略污染原始 VT 流的问题，避免 tab 恢复时把人工 marker 文本注入真实 terminal 内容。

## Changed

- `packages/sdkwork-terminal-shell/src/model.ts`
  - `runtimeTerminalContent` 截断后仅保留真实 PTY tail。
  - 保留 `runtimeContentTruncated = true` 元数据，但不再把 `"[...output truncated...]"` 写入原始 terminal payload。
- `tests/shell-tabs.test.ts`
  - 新增大体量 replay 截断回归测试，确保截断后仍保持纯 raw VT payload，且尾部 ANSI / alternate-screen 序列不被人工 marker 污染。
- 文档回写
  - `docs/架构/08-渲染、兼容性与终端体验设计.md`
  - `docs/step/05-Terminal-Core与渲染交互基线落地.md`
  - `docs/prompts/反复执行Step指令.md`
  - `docs/release/CHANGELOG.md`

## Verification

- `node --experimental-strip-types --test tests/shell-tabs.test.ts`
- `node --experimental-strip-types --test tests/shell-tabs.test.ts tests/shell-app-render.test.ts tests/terminal-view-driver.test.ts tests/desktop-runtime-bridge.test.ts`
- `pnpm typecheck`
- `pnpm build`

## Remaining Gap

- 仍需继续留在 `Step 05`，补齐 `Windows / Ubuntu / macOS` 实机 smoke 与 `TUI / CJK / IME / OSC / alternate screen` 终端行为矩阵。
