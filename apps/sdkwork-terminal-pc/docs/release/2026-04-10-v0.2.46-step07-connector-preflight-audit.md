# 2026-04-10 v0.2.46 Step 07 Connector Preflight Audit

## Summary

This round adds a repository-owned preflight audit for Step 07 connector smoke. The project can now inspect current-host readiness for `ssh / docker-exec / kubernetes-exec` before entering the real-machine smoke wave, separating environment blockers from product regressions.

## Changed

- `tools/smoke/connector-interactive-probe.mjs`
  - Added `buildConnectorInteractivePreflightReport(...)`.
  - Added CLI support for `--print-preflight --platform <platform>`.
  - The preflight report now summarizes command readiness, authority candidates, and environment blockers such as Docker daemon unavailability or missing Kubernetes context.
- `tests/connector-interactive-probe.test.ts`
  - Added repository-owned coverage for ready-host preflight, blocked-host preflight, and CLI preflight output.
- `tools/smoke/README.md`
- `README.md`
- `docs/架构/06-终端会话、运行目标与协议设计.md`
- `docs/step/07-SSH、Docker、Kubernetes与Remote-Runtime连接器落地.md`
- `docs/prompts/反复执行Step指令.md`
  - Updated Step 07 execution guidance so preflight runs before batch-plan generation and template writing.

## Verification

- `node --experimental-strip-types --test tests/connector-interactive-probe.test.ts`
- `node tools/smoke/connector-interactive-probe.mjs --print-preflight --platform windows-desktop`

## Current Host Snapshot

- `ssh`: ready
- `docker-exec`: blocked, Docker daemon unavailable on current Windows host
- `kubernetes-exec`: blocked, `kubectl current-context` is not set on current Windows host

## Remaining Gap

- Step 07 is still open.
- Preflight only distinguishes host blockers from product blockers; it does not replace real-machine smoke evidence.
- `remote-runtime` live attach/input/stream/recovery remains unimplemented.
