# 2026-04-09 v0.2.10

## 波次 / Step

- Wave B
- Step 07 进行中：完成 desktop resource launch loop 子阶段

## Changed

- `packages/sdkwork-terminal-resources/src/model.ts` 新增 `ResourceLaunchSummary`、`ResourceLaunchStatus` 与 launch 成功/失败摘要能力。
- `packages/sdkwork-terminal-resources/src/index.tsx` 新增 resource launch action、launching 状态与结构化结果面板。
- `packages/sdkwork-terminal-shell/src/index.tsx` 新增 resources 组合透传。
- `apps/desktop/src/App.tsx` 与 `apps/desktop/src/resource-launch.ts` 已接通 `ResourcesPanel -> desktop_connector_launch -> launch summary` 最小闭环。
- `apps/web/vite.config.ts`、`apps/desktop/vite.config.ts` 与 `vite.workspace-alias.ts` 对齐 workspace alias。
- 根 `package.json`、前端 apps/packages、Cargo workspace 与 `src-tauri/tauri.conf.json` 统一升级到 `0.2.10`。

## Fixed

- 修复 Resource Center 仍停留在只读展示、桌面端无法直接发起 connector launch 的问题。
- 修复 launch 结果无法在资源面板收口为统一成功/失败证据的问题。
- 修复 workspace package alias 仅在 `tsc` 可解析、Vite 构建期失效的问题。

## Architecture Alignment

- `README.md`
- `docs/架构/README.md`
- `docs/架构/05-功能架构与核心业务流程.md`
- `docs/架构/06-终端会话、运行目标与协议设计.md`
- `docs/架构/17-能力到API与IPC调用矩阵.md`
- `docs/step/README.md`
- `docs/step/07-SSH、Docker、Kubernetes与Remote-Runtime连接器落地.md`
- `docs/step/90-架构能力-Step-代码目录-证据映射矩阵.md`
- `docs/review/2026-04-09-step07-desktop-resource-launch-loop验证.md`

## Verified

- `node --experimental-strip-types --test tests/resource-center.test.ts`
- `node --experimental-strip-types --test tests/desktop-resource-launch.test.ts`
- `node --experimental-strip-types --test tests/desktop-runtime-bridge.test.ts`
- `pnpm typecheck`
- `pnpm build`

## Remaining Gaps

- 当前桌面资源 launch 闭环仍只覆盖 connect phase，尚未扩展到 exec phase。
- 远程目标的真实 attach / terminal streaming 仍未打通。
- Remote Runtime 仍未进入 broker / node 路由闭环。
- Step 07 仍未达到 `L3` 完成定义。

## Rollback

- 回退到 `v0.2.9` 可恢复 Step 07 的 desktop IPC launch bridge 基线。
- 若继续推进远程目标 Session 主链，必须继续同步更新 `docs/架构`、`docs/step`、`docs/review` 与 `docs/release`。
