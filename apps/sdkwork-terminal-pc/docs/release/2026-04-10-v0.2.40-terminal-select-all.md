# 2026-04-10 v0.2.40 Terminal Select All

## Summary

本轮继续停留在 `Wave B / Step 05`，补齐 `Ctrl/Cmd + Shift + A` 的真实 terminal 语义，修复快捷键只恢复 focus、不执行实际全选的问题。

## Changed

- `packages/sdkwork-terminal-infrastructure/src/index.ts`
  - `XtermViewportDriver` 新增 `selectAll()` 能力。
  - driver 直接调用 xterm 原生 `terminal.selectAll()`，保持 viewport selection 语义一致。
- `packages/sdkwork-terminal-shell/src/index.tsx`
  - terminal stage 的 `Ctrl/Cmd + Shift + A` 改为直接执行 `driverRef.current.selectAll()`。
  - 移除旧的 focus-only 空动作，避免用户看见快捷键存在却没有真正全选结果。
  - terminal viewport 右键菜单新增 `Select all`，与快捷键共享同一条 `selectAll()` 语义。
- 文档回写
  - `docs/架构/08-渲染、兼容性与终端体验设计.md`
  - `docs/step/05-Terminal-Core与渲染交互基线落地.md`
  - `docs/prompts/反复执行Step指令.md`
  - `docs/release/CHANGELOG.md`

## Verification

- `node --experimental-strip-types --test tests/shell-app-render.test.ts tests/terminal-view-driver.test.ts`
- `node --experimental-strip-types --test tests/shell-tabs.test.ts tests/shell-app-render.test.ts tests/terminal-view-driver.test.ts tests/desktop-runtime-bridge.test.ts`
- `pnpm typecheck`
- `pnpm build`

## Remaining Gap

- 仍需继续留在 `Step 05`，补齐 `Windows / Ubuntu / macOS` 实机 smoke 与 `TUI / CJK / IME / OSC / alternate screen` 终端行为矩阵。
- 当前已补齐 `Select All` 真实动作，但仍需继续收口更多真实终端交互证据，而不是提前切到下一阶段。
