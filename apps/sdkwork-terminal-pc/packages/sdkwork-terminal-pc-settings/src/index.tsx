import { SurfaceCard } from "@sdkwork/terminal-pc-ui";

export function SettingsPanel() {
  return (
    <SurfaceCard title="Settings">
      <p style={{ margin: 0, color: "#c8d2df" }}>
        ??????? `config.toml + SQLite` ??????????? Profile?Theme?Keybinding ??????
      </p>
    </SurfaceCard>
  );
}

