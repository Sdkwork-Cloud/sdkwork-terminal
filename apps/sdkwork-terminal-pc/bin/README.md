# Bin Scripts

Cross-platform operational scripts for the PC application workspace.

Thin wrappers for build, verification, packaging, and release should live here per `APP_PC_ARCHITECTURE_SPEC.md`. Heavy orchestration remains in `tools/scripts/` until the bin/ migration completes.
