# Public Assets

Browser static assets for the PC application root bootstrap.

Sub-surface renderers (`apps/web/`, `apps/desktop/`) may keep surface-specific assets locally during the multi-sub-app migration (**EX-2026-PC-001**). Shared root assets belong here as the single-root migration completes.
