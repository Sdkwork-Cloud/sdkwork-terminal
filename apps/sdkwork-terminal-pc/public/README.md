# Public Assets

Browser static assets for the PC application root bootstrap.

Web and desktop surfaces build from `index.web.html` / `index.desktop.html` at the PC application root. Shared static assets belong here.
