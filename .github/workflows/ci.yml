name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  workspace-verify:
    name: Workspace verify
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Enable corepack pnpm
        run: |
          corepack enable
          corepack prepare pnpm@10.30.2 --activate

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2

      - name: Install Linux desktop dependencies
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            file \
            libfuse2t64 \
            libgtk-3-dev \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libssl-dev \
            pkg-config \
            patchelf

      - name: Install workspace dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Run workspace and release contract tests
        shell: bash
        run: |
          node --experimental-strip-types tests/workspace-structure.test.mjs
          node --experimental-strip-types tests/release-plan.test.mjs
          node --experimental-strip-types tests/release-assets.test.mjs
          node --experimental-strip-types tests/release-workflows.test.mjs
          node --experimental-strip-types tests/desktop-runtime-bridge.test.ts
          node --experimental-strip-types tests/desktop-session-center.test.ts
          node --experimental-strip-types tests/shell-runtime.test.ts
          node --experimental-strip-types tests/desktop-tauri-permissions.test.ts
          node --experimental-strip-types tests/shell-app-render.test.ts
          node --experimental-strip-types tests/shell-tabs.test.ts

      - name: Run TypeScript checks
        run: pnpm typecheck

      - name: Build desktop and web workspaces
        run: pnpm build

      - name: Check Tauri desktop host
        run: cargo check --manifest-path src-tauri/Cargo.toml

  desktop-host-check:
    name: Desktop host check (${{ matrix.platform }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: windows
            runner: windows-2022
          - platform: macos
            runner: macos-14
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Enable corepack pnpm
        run: |
          corepack enable
          corepack prepare pnpm@10.30.2 --activate

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2

      - name: Install workspace dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Run desktop typecheck
        run: pnpm --dir apps/desktop typecheck

      - name: Check Tauri desktop host
        run: cargo check --manifest-path src-tauri/Cargo.toml
