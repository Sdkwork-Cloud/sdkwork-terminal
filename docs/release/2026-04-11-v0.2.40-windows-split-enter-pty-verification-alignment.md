# 2026-04-11 v0.2.40 Windows Split-Enter PTY Verification Alignment

## Scope

- Windows PTY split-enter verification
- Local PTY vs host-level timing alignment
- False-negative test signal removal
- Terminal interaction confidence tightening

## Changed

- `crates/sdkwork-terminal-pty-runtime/tests/local_shell_session_runtime.rs`
  - Increased the Windows split-enter output collection window from 8 seconds to 10 seconds.
  - Added a note explaining that Windows PowerShell can spend extra time repainting split-enter input before final output lands on the PTY stream.

## Fixed

- Fixed a false-negative local PTY verification failure where `command` and `Enter` sent separately could fail in the standalone PTY test even though the host-level `runtime-node` path already passed the same interaction.
- Re-aligned local PTY verification timing with the current desktop/server host test envelope.

## Verified

- `cargo test -p sdkwork-terminal-pty-runtime --test local_shell_session_runtime -- --nocapture`
- `cargo test -p sdkwork-terminal-runtime-node --test runtime_node_host_test -- --nocapture`

## Notes

- This increment changes verification timing only; it does not change PTY runtime behavior, frontend input routing, or product versions.
- The next highest-value step remains live desktop PTY interaction smoke inside the real Tauri terminal surface.
