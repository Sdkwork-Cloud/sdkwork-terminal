# 2026-04-09 v0.2.12

## 波次 / Step

- Wave B
- Step 07 进行中：完成 desktop session index bridge 与 Session Center runtime projection 子阶段

## Changed

- `packages/sdkwork-terminal-infrastructure/src/index.ts` 新增 `DesktopSessionIndexSnapshot` 与 `sessionIndex` bridge。
- `src-tauri/src/lib.rs` 新增 `DesktopSessionDescriptorSnapshot`、`DesktopAttachmentDescriptorSnapshot`、`DesktopSessionIndexSnapshot` 与 `desktop_session_index`。
- `apps/desktop/src/session-center.ts` 已新增 runtime session index -> Session Center snapshot 映射；`apps/desktop/src/App.tsx` 已接通启动加载与 launch / probe 后刷新。
- `packages/sdkwork-terminal-shell/src/index.tsx` 已支持透传真实 `sessions` snapshot。
- 根 `package.json`、前端 apps/packages、Cargo workspace 与 `src-tauri/tauri.conf.json` 统一升级到 `0.2.12`。

## Fixed

- 修复 `SessionsPanel` 仍停留在 demo 数据、看不到 launch / exec probe 后真实 runtime session 的问题。
- 修复桌面端缺少 session index IPC、导致 Resource Center 与 Session Center 事实割裂的问题。
- 修复 session truth 已在 runtime 中存在，但桌面 UI 未消费该 truth 的缺口。

## Architecture Alignment

- `README.md`
- `docs/架构/README.md`
- `docs/架构/05-功能架构与核心业务流程.md`
- `docs/架构/06-终端会话、运行目标与协议设计.md`
- `docs/架构/17-能力到API与IPC调用矩阵.md`
- `docs/step/README.md`
- `docs/step/07-SSH、Docker、Kubernetes与Remote-Runtime连接器落地.md`
- `docs/step/90-架构能力-Step-代码目录-证据映射矩阵.md`
- `docs/review/2026-04-09-step07-desktop-session-index-bridge验证.md`

## Verified

- `node --experimental-strip-types --test tests/desktop-runtime-bridge.test.ts tests/desktop-resource-launch.test.ts tests/resources-panel.test.ts tests/desktop-session-center.test.ts tests/session-center.test.ts`
- `cargo test -p sdkwork-terminal-session-runtime`
- `cargo test --manifest-path src-tauri/Cargo.toml`
- `pnpm typecheck`
- `pnpm build`

## Remaining Gaps

- 当前 `SessionsPanel` 仍是读链投影，尚未提供 attach / detach / replay 操作入口。
- 远程目标的 interactive terminal attach、实时 streaming 与 Remote Runtime broker / node 路由仍未闭环。
- Step 07 仍未达到 `L3` 完成定义。

## Rollback

- 回退到 `v0.2.11` 可恢复 Step 07 的 desktop exec probe bridge 基线。
- 若继续推进远程目标 interactive 主链，必须继续同步更新 `docs/架构`、`docs/step`、`docs/review` 与 `docs/release`。
