# 2026-04-09 v0.2.1

## 波次 / Step

- Wave B
- Step 07 进行中：完成 contract freeze、connector catalog、Resource Center model

## Changed

- `packages/sdkwork-terminal-types` 新增 `ExecutionTargetDescriptor`、connector health、connector transport 类型。
- `packages/sdkwork-terminal-contracts` 新增默认执行目标描述、Step 07 contract 字段与 snapshot 回写。
- `packages/sdkwork-terminal-resources` 新增 Resource Center 派生模型与状态展示。
- `crates/sdkwork-terminal-resource-connectors` 新增 SSH / Docker Exec / Kubernetes Exec / Remote Runtime catalog 与 summary。
- `crates/sdkwork-terminal-protocol` 与测试 fixture 同步 Step 07 contract。
- 根 `package.json`、前端 apps/packages、Cargo workspace 与 `src-tauri/tauri.conf.json` 统一升级到 `0.2.1`。

## Fixed

- 修复 Resources 面板只显示静态占位文案、无法反映目标状态的问题。
- 修复共享 contract 缺失执行目标描述字段，导致 Resource Center 无法以统一模型消费状态的问题。
- 修复 Rust resource-connectors crate 无法提供可验证目录事实的问题。
- 修复版本回写过程写入 JSON BOM，导致 workspace 测试、Vite 构建与 Tauri 配置解析失败的问题。

## Architecture Alignment

- `docs/架构/README.md`
- `docs/架构/04-技术选型与可插拔策略.md`
- `docs/架构/05-功能架构与核心业务流程.md`
- `docs/架构/06-终端会话、运行目标与协议设计.md`
- `docs/架构/17-能力到API与IPC调用矩阵.md`
- `docs/step/README.md`
- `docs/step/07-SSH、Docker、Kubernetes与Remote-Runtime连接器落地.md`
- `docs/step/90-架构能力-Step-代码目录-证据映射矩阵.md`

## Verified

- `node --experimental-strip-types --test tests/resource-center.test.ts`
- `node --experimental-strip-types --test tests/runtime-contracts.test.ts`
- `cargo test -p sdkwork-terminal-resource-connectors`
- `cargo test -p sdkwork-terminal-protocol`

## Remaining Gaps

- 真实 SSH / Docker / Kubernetes `connect / exec / close / diag` 主链仍未落地。
- Remote Runtime 仍是 catalog 与 contract 占位，未形成 broker / node 路由闭环。
- Step 07 尚未达到 `L3` 完成定义，当前只完成 `CP07-1` 与 `CP07-4` 的基础子阶段。

## Rollback

- 回退到 `v0.2.0` 可恢复 Step 06 runtime foundation 基线。
- 若后续 Step 07 继续扩展 connector 主链，必须同步更新 `docs/架构`、`docs/step`、`docs/review` 与 `docs/release`。
