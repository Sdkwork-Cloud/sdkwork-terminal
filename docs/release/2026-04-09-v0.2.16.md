# 2026-04-09 v0.2.16

## 波次 / Step

- Wave B
- Step 07 进行中：完成 Session Center replay cursor metadata 子阶段

## Changed

- `packages/sdkwork-terminal-sessions/src/model.ts` 新增 `replayCursor` 与 `summarizeSessionReplayCursor`，让 Session Center 为 loaded replay 保留 `fromCursor / nextCursor / hasMore / window` 读模型。
- `packages/sdkwork-terminal-sessions/src/index.tsx` 已在 `SessionsPanel` 展示 replay cursor metadata 文本，和 replay status / transcript 并列呈现。
- `tests/session-center.test.ts`、`tests/desktop-session-center.test.ts` 与 `tests/sessions-panel.test.ts` 已补齐 replay cursor projection 断言。
- 根 `package.json`、前端 apps/packages、Cargo workspace 与 `src-tauri/tauri.conf.json` 统一升级到 `0.2.16`。

## Fixed

- 修复 Session Center 已可显示 replay status，但仍无法显式读取 cursor 窗口摘要的问题。
- 修复 loaded replay 只能展示 transcript 与 status、无法读出 `fromCursor / nextCursor / hasMore` 事实的问题。
- 修复 Step 07 文档未同步 replay cursor metadata projection 事实的问题。

## Architecture Alignment

- `README.md`
- `docs/架构/README.md`
- `docs/架构/05-功能架构与核心业务流程.md`
- `docs/架构/06-终端会话、运行目标与协议设计.md`
- `docs/架构/17-能力到API与IPC调用矩阵.md`
- `docs/step/README.md`
- `docs/step/07-SSH、Docker、Kubernetes与Remote-Runtime连接器落地.md`
- `docs/step/90-架构能力-Step-代码目录-证据映射矩阵.md`
- `docs/review/2026-04-09-step07-session-replay-cursor-metadata验证.md`

## Verified

- `node --experimental-strip-types --test tests/desktop-runtime-bridge.test.ts tests/desktop-resource-launch.test.ts tests/resources-panel.test.ts tests/desktop-session-center.test.ts tests/session-center.test.ts tests/sessions-panel.test.ts`
- `pnpm test`
- `pnpm typecheck`
- `pnpm build`
- `cargo test -p sdkwork-terminal-session-runtime`
- `cargo test --manifest-path src-tauri/Cargo.toml`

## Remaining Gaps

- 当前 `SessionsPanel` 已能显示 bounded transcript、replay unavailable 诊断与 replay cursor metadata，但仍未提供 attach / detach / replay 游标控制入口。
- 远程目标 interactive terminal attach、实时 streaming 与 Remote Runtime broker / node 主链仍未闭环。
- Step 07 仍未达到 `L3` 完成定义。

## Rollback

- 回退到 `v0.2.15` 可恢复 Step 07 的 replay status diagnostics 基线。
- 若继续推进 interactive attach / replay cursor 主链，必须继续同步更新 `docs/架构`、`docs/step`、`docs/review` 与 `docs/release`。
