# 2026-04-10 v0.2.40 Step 07 Connector Interactive Smoke Baseline

## Summary

为 Step 07 `CP07-4` 建立仓库级 `docker-exec / kubernetes-exec` live terminal smoke 基线。当前仓库已具备共享 control-plane 自动化覆盖，以及可重复生成的 smoke 计划、JSON report template 和 markdown review template。

## Changed

- 新增 `tools/smoke/connector-interactive-probe.mjs` 与 `connector-interactive-probe.ps1`。
- 新增 `tests/connector-interactive-probe.test.ts`，验证 smoke plan、report template、review template 与 CLI 输出。
- `crates/sdkwork-terminal-control-plane` 已新增 `docker-exec / kubernetes-exec` interactive bootstrap 自动化覆盖。
- README、架构 06、Step 07、Step 90 与 `tools/smoke/README.md` 已同步回写。

## Verified

- `node --experimental-strip-types --test tests/connector-interactive-probe.test.ts`
- `cargo test --manifest-path crates/sdkwork-terminal-control-plane/Cargo.toml -- --nocapture`

## Constraint

- 本轮只关闭 `CP07-4` 的 smoke baseline 缺口，不宣称实机 Docker/Kubernetes report/review、connector attach/reconnect/recovery 或 Remote Runtime recovery 已完成。
