# 2026-04-10 v0.2.40 Terminal Selection Copy

## Summary

本轮继续停留在 `Wave B / Step 05`，补齐 tab 级上下文菜单 `Copy` 与真实 terminal viewport 的统一语义，避免菜单复制读取陈旧快照而不是当前 xterm selection。

## Changed

- `packages/sdkwork-terminal-shell/src/index.tsx`
  - 新增 `viewportCopyHandlersRef`，由 shell 容器按 tab 维护 viewport copy handler。
  - tab 菜单 `Copy` 改为优先读取 `contextMenu.tabId` 对应 viewport 的实时 selection。
  - 仅当目标 tab 当前没有挂载 viewport handler 时，才回退到 `copiedText` snapshot。
- `TerminalStage`
  - 新增 `onRegisterViewportCopyHandler(...)` 注册链路。
  - copy handler 直接读取 `driverRef.current.getSelection()` 并写入 clipboard，保持与真实 xterm viewport 对齐。
- 文档回写
  - `docs/架构/08-渲染、兼容性与终端体验设计.md`
  - `docs/step/05-Terminal-Core与渲染交互基线落地.md`
  - `docs/prompts/反复执行Step指令.md`
  - `docs/release/CHANGELOG.md`

## Verification

- `node --experimental-strip-types --test tests/shell-app-render.test.ts tests/terminal-view-driver.test.ts`
- `node --experimental-strip-types --test tests/shell-tabs.test.ts tests/shell-app-render.test.ts tests/terminal-view-driver.test.ts tests/desktop-runtime-bridge.test.ts`
- `pnpm typecheck`
- `pnpm build`
- `pnpm dev`
  - 进程在超时前保持运行，未见立即启动崩溃。

## Remaining Gap

- 仍需继续留在 `Step 05`，补齐真实终端输入、TUI、IME、CJK、OSC、alternate screen 的跨平台实机验证。
- 当前已统一 copy/paste 的 viewport-first 语义，但仍需在 Windows / Ubuntu / macOS 实机确认 tmux、vim、shell selection 的最终体验证据。
