# 2026-04-10 v0.2.48 Step 07 Remote Runtime Web Bridge

## Summary

This round closes the missing Step 07 foundation slice between `remote-runtime` discovery and a future real web/runtime-node terminal host. The repository now has a dedicated `remote-runtime` create-request model and a web bridge client that maps session create, replay, input, resize, terminate, and stream subscribe onto the approved `publicApi` and `runtimeStream` surfaces.

## Changed

- `packages/sdkwork-terminal-types/src/index.ts`
  - Added `RemoteRuntimeSessionCreateRequest`.
- `packages/sdkwork-terminal-resources/src/model.ts`
  - Added `createRemoteRuntimeSessionCreateRequest(...)`.
  - Kept `createConnectorSessionLaunchRequest(...)` restricted to desktop `system-cli` targets.
- `packages/sdkwork-terminal-infrastructure/src/index.ts`
  - Added generic runtime-session aliases for replay/input/resize/terminate snapshots.
  - Added `WebFetch`, `WebEventSourceFactory`, `WebRuntimeBridgeClient`.
  - Added `createWebRuntimeBridgeClient(...)`.
- `tests/resource-center.test.ts`
  - Added coverage for the dedicated `remote-runtime` request path.
- `tests/web-runtime-bridge.test.ts`
  - Added coverage for `publicApi` request routing.
  - Added coverage for `runtimeStream /attach` event subscription.
- `README.md`
- `docs/架构/06-终端会话、运行目标与协议设计.md`
- `docs/step/07-SSH、Docker、Kubernetes与Remote-Runtime连接器落地.md`
- `docs/prompts/反复执行Step指令.md`

## Verification

- `node --experimental-strip-types --test tests/resource-center.test.ts tests/web-runtime-bridge.test.ts`
- `node --experimental-strip-types --test tests/runtime-contracts.test.ts tests/desktop-runtime-bridge.test.ts tests/desktop-resource-launch.test.ts tests/resource-center.test.ts tests/web-runtime-bridge.test.ts`

## Remaining Gap

- Step 07 is still open.
- `apps/web -> ShellApp` has not been rewired to this bridge yet.
- No real runtime-node host/API implementation was added in this slice.
- `remote-runtime` live attach/input/stream/recovery still requires host-side closure and evidence.
