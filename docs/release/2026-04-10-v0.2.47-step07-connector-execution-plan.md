# 2026-04-10 v0.2.47 Step 07 Connector Execution Plan

## Summary

This round adds the missing execution layer on top of Step 07 connector smoke. The repository can now merge host preflight and the platform-target batch matrix into a single execution plan, then optionally generate report/review templates only for targets that are actually executable on the current host.

## Changed

- `tools/smoke/connector-interactive-probe.mjs`
  - Added `buildConnectorInteractiveExecutionPlan(...)`.
  - Added CLI support for `--print-execution-plan --platform <platform>`.
  - Added `--ready-only` filtering for `--write-batch-templates`.
  - Batch-template output now returns `selection` metadata and `skippedEntries`.
- `tests/connector-interactive-probe.test.ts`
  - Added execution-plan merge coverage.
  - Added ready-only template filtering coverage.
  - Added CLI execution-plan coverage.
  - Added smoke README coverage for `print-execution-plan` and `ready-only`.
- `tools/smoke/README.md`
- `README.md`
- `docs/架构/06-终端会话、运行目标与协议设计.md`
- `docs/step/07-SSH、Docker、Kubernetes与Remote-Runtime连接器落地.md`
- `docs/prompts/反复执行Step指令.md`
  - Updated Step 07 guidance to use the sequence `preflight -> execution-plan -> ready-only templates -> field smoke -> serial closeout`.

## Verification

- `node --experimental-strip-types --test tests/connector-interactive-probe.test.ts`
- `node tools/smoke/connector-interactive-probe.mjs --print-execution-plan --platform windows-desktop`
- `node tools/smoke/connector-interactive-probe.mjs --write-batch-templates --platform windows-desktop --output-dir %TEMP%/sdkwork-terminal-ready-only-cli --ready-only`

## Current Host Snapshot

- `windows-desktop:ssh`: executable now
- `windows-desktop:docker-exec`: skipped, Docker daemon unavailable on current Windows host
- `windows-desktop:kubernetes-exec`: skipped, `kubectl current-context` is not set on current Windows host

## Remaining Gap

- Step 07 is still open.
- Execution plan and ready-only filtering do not replace real-machine smoke evidence.
- `remote-runtime` live attach/input/stream/recovery remains unimplemented.
