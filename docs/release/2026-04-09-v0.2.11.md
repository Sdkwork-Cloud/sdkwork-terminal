# 2026-04-09 v0.2.11

## 波次 / Step

- Wave B
- Step 07 进行中：完成 desktop exec probe bridge 与 desktop resource exec probe loop 子阶段

## Changed

- `packages/sdkwork-terminal-resources/src/model.ts` 新增 `ResourceActionKind`、`createConnectorExecProbeRequest`、`createResourceTargetActions`、`createResourceExecProbeSummary` 与 `createResourceExecProbeFailureSummary`。
- `packages/sdkwork-terminal-resources/src/index.tsx` 已支持 launch / exec probe 双动作，以及动作感知的 pending / retry 标签。
- `apps/desktop/src/resource-launch.ts` 已新增 `probeDesktopResourceTarget`；`apps/desktop/src/App.tsx` 已接通 `ResourcesPanel -> desktop_connector_exec_probe -> exec probe summary`。
- 根 `package.json`、前端 apps/packages、Cargo workspace 与 `src-tauri/tauri.conf.json` 统一升级到 `0.2.11`。

## Fixed

- 修复桌面资源面板仍只能发起 connector launch、无法消费已存在 `desktop_connector_exec_probe` IPC 的问题。
- 修复 Resource Center 缺少 exec probe 动作态、导致按钮标签与 pending 状态无法精确表达的缺口。
- 修复 exec probe 结果无法稳定投影为 `exitCode / output / warning` 结构化摘要的问题。

## Architecture Alignment

- `README.md`
- `docs/架构/README.md`
- `docs/架构/04-技术选型与可插拔策略.md`
- `docs/架构/05-功能架构与核心业务流程.md`
- `docs/架构/06-终端会话、运行目标与协议设计.md`
- `docs/架构/17-能力到API与IPC调用矩阵.md`
- `docs/step/README.md`
- `docs/step/07-SSH、Docker、Kubernetes与Remote-Runtime连接器落地.md`
- `docs/step/90-架构能力-Step-代码目录-证据映射矩阵.md`
- `docs/review/2026-04-09-step07-desktop-exec-probe-bridge验证.md`

## Verified

- `node --experimental-strip-types --test tests/desktop-runtime-bridge.test.ts tests/desktop-resource-launch.test.ts tests/resources-panel.test.ts`
- `cargo test -p sdkwork-terminal-session-runtime`
- `cargo test --manifest-path src-tauri/Cargo.toml`
- `pnpm typecheck`
- `pnpm build`

## Remaining Gaps

- 当前 exec probe 仍是 bounded `connect + exec` 证据回写，不是 interactive terminal attach。
- 远程目标的实时 streaming、attach / detach 与 Remote Runtime broker / node 路由仍未闭环。
- Step 07 仍未达到 `L3` 完成定义。

## Rollback

- 回退到 `v0.2.10` 可恢复 Step 07 的 desktop resource launch loop 基线。
- 若继续推进远程目标 interactive 主链，必须继续同步更新 `docs/架构`、`docs/step`、`docs/review` 与 `docs/release`。
