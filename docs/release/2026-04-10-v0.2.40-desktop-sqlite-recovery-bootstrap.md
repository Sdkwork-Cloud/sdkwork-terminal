# 2026-04-10 v0.2.40 Desktop SQLite Recovery Bootstrap

## Summary

- 桌面端 `SessionRuntime` 的 SQLite bootstrap 已下沉到共享 Rust `sdkwork-terminal-control-plane`。
- `src-tauri` 现在只负责解析 `app_local_data_dir()` 并桥接到固定文件名 `session-runtime.sqlite3`，不再维护第二套私有恢复初始化逻辑。
- 当前 Windows 开发主机上的 `src-tauri` Rust 单测二进制仍受 `STATUS_ENTRYPOINT_NOT_FOUND` 宿主加载限制；本轮以共享 Rust 控制面测试作为恢复主证据，以 `src-tauri cargo check` 作为桌面接线证据。

## Files

- `crates/sdkwork-terminal-control-plane/Cargo.toml`
- `crates/sdkwork-terminal-control-plane/src/lib.rs`
- `src-tauri/Cargo.toml`
- `src-tauri/src/lib.rs`
- `README.md`
- `docs/架构/06-终端会话、运行目标与协议设计.md`
- `docs/step/06-Session-Runtime、Replay与Recovery主链落地.md`
- `docs/prompts/反复执行Step指令.md`

## Verification

- `cargo test --manifest-path crates/sdkwork-terminal-control-plane/Cargo.toml -- --nocapture`
- `cargo check --manifest-path src-tauri/Cargo.toml`
- `node --experimental-strip-types --test tests/desktop-runtime-bridge.test.ts tests/desktop-session-center.test.ts tests/desktop-session-reattach.test.ts tests/shell-tabs.test.ts tests/shell-app-render.test.ts tests/terminal-fidelity-probe.test.ts`
- `pnpm typecheck`

## Constraint

- 直接执行 `cargo test --manifest-path src-tauri/Cargo.toml ...` 在当前 Windows 主机仍会命中 `STATUS_ENTRYPOINT_NOT_FOUND`；该限制已被显式记录，不得把它误报成 Step 06 业务逻辑失败，也不得把 `--no-run` 误报成行为通过。
