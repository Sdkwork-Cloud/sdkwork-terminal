# 2026-04-09 v0.2.3

## 波次 / Step

- Wave B
- Step 07 进行中：完成 runner 执行抽象与 system smoke 子阶段

## Changed

- `crates/sdkwork-terminal-resource-connectors` 新增 `ConnectorPhase`、`CommandRunner`、`SystemCommandRunner`、`execute_plan_phase`。
- connector 执行错误已具备 `code / phase / retryable / status / program` 归类。
- Rust connector 主链已从 launch plan 推进到 runner 可执行，并完成本机 smoke。
- README、架构、Step、review、release 文档已同步回写到当前真实状态。
- 根 `package.json`、前端 apps/packages、Cargo workspace 与 `src-tauri/tauri.conf.json` 统一升级到 `0.2.3`。

## Fixed

- 修复 connector 停留在计划层、无法进入执行层验证的问题。
- 修复 connector 执行失败缺少 phase 与 retryable 归类的问题。
- 修复 Windows smoke 场景下 stderr 尾随空白导致断言不稳定的问题。

## Architecture Alignment

- `docs/架构/README.md`
- `docs/架构/04-技术选型与可插拔策略.md`
- `docs/架构/05-功能架构与核心业务流程.md`
- `docs/架构/17-能力到API与IPC调用矩阵.md`
- `docs/step/README.md`
- `docs/step/07-SSH、Docker、Kubernetes与Remote-Runtime连接器落地.md`
- `docs/step/90-架构能力-Step-代码目录-证据映射矩阵.md`

## Verified

- `node --test tests/workspace-structure.test.mjs`
- `node --experimental-strip-types --test tests/runtime-contracts.test.ts`
- `node --experimental-strip-types --test tests/terminal-core-workbench.test.ts`
- `node --experimental-strip-types --test tests/session-center.test.ts`
- `node --experimental-strip-types --test tests/resource-center.test.ts`
- `pnpm typecheck`
- `pnpm build`
- `cargo test --workspace`
- `cargo check --manifest-path src-tauri/Cargo.toml`

## Remaining Gaps

- 真实 SSH / Docker / Kubernetes 工具链 smoke 仍未落地。
- Remote Runtime 仍未进入 broker / node 路由闭环。
- Step 07 仍未达到 `L3` 完成定义。

## Rollback

- 回退到 `v0.2.2` 可恢复 Step 07 的 session draft + launch plan 基线。
- 若继续推进真实工具链 smoke，必须继续同步更新 `docs/架构`、`docs/step`、`docs/review` 与 `docs/release`。
