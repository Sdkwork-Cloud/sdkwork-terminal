# 2026-04-10 v0.2.45 Step 07 Connector Batch Template Writer

## Summary

This round turns the Step 07 connector batch matrix into a writable template tool. The repository can now emit report/review skeleton files for a filtered platform batch, reducing the manual setup cost before real-machine `ssh / docker-exec / kubernetes-exec` smoke execution.

## Changed

- `tools/smoke/connector-interactive-probe.mjs`
  - Added `writeConnectorInteractiveBatchTemplates(...)`.
  - Added CLI support for `--write-batch-templates --output-dir <dir>`.
  - The tool now writes JSON report templates and markdown review templates for every selected `platform-target` entry.
- `tests/connector-interactive-probe.test.ts`
  - Added repository-owned coverage for direct template writing and CLI-driven batch template output.
- `tools/smoke/README.md`
- `README.md`
- `docs/架构/06-终端会话、运行目标与协议设计.md`
- `docs/step/07-SSH、Docker、Kubernetes与Remote-Runtime连接器落地.md`
- `docs/prompts/反复执行Step指令.md`
  - Updated the Step 07 execution path so batch planning is followed by batch template generation before field smoke execution.

## Verification

- `node --experimental-strip-types --test tests/connector-interactive-probe.test.ts`
- `node tools/smoke/connector-interactive-probe.mjs --write-batch-templates --platform ubuntu-desktop --output-dir $env:TEMP/sdkwork-terminal-connector-batch-manual`

## Remaining Gap

- Step 07 is still open.
- The new writer reduces template setup cost only; it does not replace real-machine smoke execution or archived reviewed evidence.
- `remote-runtime` live attach/input/stream/recovery remains unimplemented.
