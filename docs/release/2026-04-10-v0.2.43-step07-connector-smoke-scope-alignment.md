# 2026-04-10 v0.2.43 Step 07 Connector Smoke Scope Alignment

## Summary

This round closes the Step 07 documentation drift around the repository-owned connector smoke entry. `connector-interactive-probe` is now frozen in the project docs as the shared evidence path for `ssh / docker-exec / kubernetes-exec` live terminal, Session Center reattach, and restart/recovery review, rather than a Docker/Kubernetes-only launch note.

## Changed

- `README.md`
  - Updated the current Step 07 truth so the probe scope is explicitly `ssh / docker-exec / kubernetes-exec` live terminal plus Session Center reattach and restart/recovery evidence.
- `docs/架构/06-终端会话、运行目标与协议设计.md`
  - Aligned the architecture baseline with the real connector smoke scope and the remaining gap definition.
- `docs/step/07-SSH、Docker、Kubernetes与Remote-Runtime连接器落地.md`
  - Rewrote the active Step 07 evidence wording so the next action is real-machine report/review closure across `ssh / docker-exec / kubernetes-exec`.
- `docs/prompts/反复执行Step指令.md`
  - Added a Step 07 guardrail that forbids claiming connector closure from launch-only notes and requires using the repository-owned smoke entry.
- `docs/review/2026-04-10-step07-connector-smoke-scope-alignment验证.md`
  - Added a matching review record for this evidence-baseline alignment.

## Verification

- `node --experimental-strip-types --test tests/connector-interactive-probe.test.ts`
- `node tools/smoke/connector-interactive-probe.mjs --print-plan`

## Remaining Gap

- Step 07 is still open.
- `ssh / docker-exec / kubernetes-exec` still need real-machine report/review artifacts for reattach/recovery closure.
- `remote-runtime` live attach/input/stream/recovery remains unimplemented.
