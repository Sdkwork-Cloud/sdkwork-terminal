[package]
name = "sdkwork-terminal-config"
version.workspace = true
edition.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
sdkwork-terminal-protocol = { path = "../sdkwork-terminal-protocol" }
